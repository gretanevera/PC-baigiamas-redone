/*
 * CategoryPage2 Messages
 *
 * This contains all the text for the CategoryPage2 container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.CategoryPage2';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the CategoryPage2 container!',
  },
});
