// import { selectCategoryPage2Domain } from '../selectors';

describe('selectCategoryPage2Domain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
