/*
 * CategoryPage Messages
 *
 * This contains all the text for the CategoryPage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.CategoryPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the CategoryPage container!',
  },
});
